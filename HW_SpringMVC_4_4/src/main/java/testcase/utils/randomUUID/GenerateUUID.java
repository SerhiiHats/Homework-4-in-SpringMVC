package testcase.utils.randomUUID;

import java.util.UUID;

public class GenerateUUID {

    public static UUID getRandomUuid(){
//    UUID uuid = UUID.randomUUID();
    return UUID.randomUUID();
    }
}
